# Security Configuration Guidelines
# This file documents security best practices for environment variables

# SESSION_SECRET Requirements:
# - MUST be at least 32 bytes (64 hex characters)
# - Generate with: openssl rand -hex 32
# - NEVER commit actual values to git
# - Rotate quarterly in production
SESSION_SECRET=CHANGE_ME_use_openssl_rand_hex_32_to_generate

# GitHub OAuth Security:
# - Use different OAuth apps for dev/staging/prod
# - Restrict callback URLs to specific domains
# - Review and minimize requested scopes
GITHUB_CLIENT_ID=dev-only-client-id
GITHUB_CLIENT_SECRET=dev-only-client-secret
GITHUB_REDIRECT_URL=http://localhost:8080/auth/callback

# Database Security:
# - Use full paths for SQLite files
# - Ensure data/ directory has restricted permissions (700)
# - Enable WAL mode for better concurrent access
# - Consider encrypting database at rest in production
DATABASE_URL=file:./data/gproject.db?_journal_mode=WAL&_busy_timeout=5000

# Production Security Settings:
# ENVIRONMENT=production  # Enables HSTS, secure cookies
# PORT=8080              # Run behind reverse proxy on non-privileged port
# LOG_LEVEL=info         # Don't log sensitive data in production

# Additional Security Headers (for reverse proxy):
# X-Real-IP: Configure in Caddy/nginx
# X-Forwarded-For: For accurate rate limiting
# X-Request-ID: For request tracing

# Recommended Caddy Configuration:
# {
#   reverse_proxy localhost:8080 {
#     header_up X-Real-IP {remote_host}
#     header_up X-Forwarded-For {remote_host}
#   }
# }